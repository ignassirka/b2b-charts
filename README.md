# B2B chart style exploration

Design playground for the chart visual language. No backend, no real data.

1. `npm install`
2. `npm run dev` → http://localhost:5173
3. Tabs switch chart family; the right-hand rail (drawer under 1280px) restyles everything live.
4. All colour lives in `src/theme/tokens.ts` — nothing else may contain a hex literal.
